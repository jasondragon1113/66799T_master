# 調參版使用說明（66799T）

這個 repo 有兩個程式版本，共用同一份程式碼，用建置指令區分：

| 版本 | 建置指令 | 燒錄槽 | 用途 |
|---|---|---|---|
| 比賽版 | `pros make comp` | slot 1 | 平常練習、正式比賽 |
| 調參版 | `pros make tune` | slot 2 | 接 dashboard 調 PID / 前饋 / 測試動作 |

## 燒調參版（最常用）

在 VS Code 開這個資料夾，終端機跑：

```bash
pros make tune
```

```bash
pros upload --slot 2 --name "66799T TUNE"
```

燒完在 Brain 螢幕選 **slot 2** 的程式執行。開機畫面和遙控器會顯示 **TUNE** 字樣＝確認跑的是調參版。

## 燒比賽版

```bash
pros make comp
```

```bash
pros upload --slot 1 --name "66799T"
```

## ⚠️ 鐵則：燒之前一定先跑對應的 make

資料夾裡現成的 bin/ 可能是另一個版本留下的——**不跑 make 直接 upload 會燒錯版本**
（最壞情況：比賽槽被燒進調參程式）。兩行一起跑就永遠不會錯。

## 連 Dashboard

1. ESP32 盒子接 Brain 的 **11 號埠**，車開機
2. 筆電 WiFi 連 `V5_Telemetry_Bridge ver1`（密碼 `vexdash12`）
3. 開 https://vex-v5.dproboticslab.com/dashboard →連線方式選 **WebSocket**→
   位址 `ws://192.168.4.1:80` → 連線
4. 剛連上時頻道清單會花 1~2 秒慢慢長齊（正常，防斷線設計）

## 調參流程、按鍵表、滑桿清單

全部在 **`TUNE_GUIDE.md`**——鍵位（B/Y/X/A 滑軌四段、方向鍵手臂四位置、
L1/L2 直線、R1/R2 轉向）、每個動作的 kP/kI/kD/startI/settle 滑桿、
前饋量測步驟、驗收清單都在裡面。

> 滑桿調出來的值**重開機會回預設**——調到滿意記得抄下來請人寫回程式碼當新預設。
